import { Box, styled } from "@mui/material";

const FooterBlock = styled('div')({
  padding: '2rem',
  
})

export function Footer() {
  return (
    <FooterBlock>
      푸터
    </FooterBlock>
  );
}
